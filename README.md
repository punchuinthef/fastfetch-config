# fastfetch-config
A simple, yet colourful FastFetch configuration.

-- The ASCII Art -- 
There is ASCII art included in this repo. I would put it in the root of your home (~) folder; otherwise, you will need to go in and manually edit the config file to change where you stored the .txt file. 
